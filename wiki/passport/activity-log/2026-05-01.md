---
title: "2026-05-01 Passport 활동 로그"
date: 2026-05-01
tags: [passport, log, merge, release]
doc_type: log
status: historical
---
# 2026-05-01 Passport 활동 로그

## Session 1 — UI 정렬 마무리 + PR #2 master 머지
- 작업 주체: Claude Opus 4.7
- 작업 내용:
  - BmuDataPage `Live data 조회` 버튼에 `alignSelf: flex-end` 적용 (label+input 묶음의 세로 중앙이 아닌 input 하단 라인에 정확히 정렬)
  - PR #2 (`velkern-brand-assets → master`) 머지 진행
    - GitHub rebase merge가 정책상 막혀있어 standard merge commit 방식 선택
    - master HEAD: `578017f → f8a7e3c` (Merge pull request #2)
    - 114개 atomic commit 히스토리 모두 보존, 옛 master `578017f`은 ancestor로 잔존 → bisect 가능
    - velkern-brand-assets 브랜치는 원격/로컬 모두 삭제 (auto-delete)
- 변경 파일: `webapp/frontend-react/src/pages/BmuDataPage.tsx`
- 커밋: `f58d3d8`, 머지 커밋 `f8a7e3c`.

## Merge에 포함된 주요 변경 (4-27 ~ 4-29 누적)
다세션 합의 기반 frontend/agent 정합 작업이 master 표준으로 승격됨.

- **페이지 레이아웃 통일**
  - 모든 페이지 헤드를 dashboard `vk-dash__head` 스타일로 정합 (min-height 54, gap 24)
  - 영문 eyebrow 9개 + section card 영문 라벨 38개 한국어 번역
  - PageHead actions에서 `sn-kpi-mini` 박스 제거, KPI 정보는 본문 카드로 이동
  - 여권 상세 페이지 maxWidth 1320 + 광폭 모니터에서 actions 우측 정렬
  - 사이드바 다크 모드 토글 CSS 보정
- **버튼 디자인**
  - sn-btn-ghost 색상을 accent 톤 secondary로 (배경 surface-accent, hover 시 accent 풀 채움)
- **대시보드 보안 카드**
  - AES/CMAC/Ed25519 항목을 BMU 레코드 `signature` 필드 + 60초 윈도우로 자동 활성화
  - 배터리 디지털 트윈 → 배터리 모니터 (자연스러운 표현)
  - 배터리 선택 버튼 라벨에 선택된 배터리 모델명 노출
- **데이터 흐름 (다세션 합의 option C)**
  - cloud-agent fabric-listener에 BMU projection 추가 (chaincode 변경 없이 read model 측에서 currentSoc/currentTemperature 등 갱신)
  - bmu-agent `/api/realtime/*` 프록시 라우트 신설 (JWT 인증 후 X-API-Key로 cloud-agent 위임)
  - DashboardPage / PassportDetailPage / BmuDataPage 읽기 endpoint를 `/realtime/...`로 전환
  - cloud-agent `GET /api/bmu/:idOrDid`에 `$or` 매칭으로 DID 검색 지원
- **BMU 인코딩 정합**
  - BMS_SecureComm.dbc 기반 SOC factor 0.001525902 (raw uint16/65535 × 100), Temperature factor 0.000762951 (raw uint16/65535 × 50) 적용
  - bmu-agent `POST /api/bmu/data`에 signature 형식 검증(128 hex) 추가
- **dev seed**
  - 100건 다양화 시드 (5종 화학, 8개 제조사, 6종 상태) 추가 후, 실 BMU 테스트 직전 비움
  - GET /:id, /:id/history에 시드 분기 추가하여 목록/상세 정합
- **README**
  - KPI 수치 섹션 제거 + 전체 정리

## 미완료/주의
- master에 대규모 변경(52개 frontend 파일, +6,663/-1,665 라인) 한 번에 반영됨. 다른 머신에서 `git pull` 시 npm 빌드 필요.
- 운영 배포 시 cloud-agent는 `CLOUD_AGENT_API_KEY` env 설정 필수 (gitignored .env 별도 배포)
- e2e fixture는 ledger reset 후 재구성 필요 (블록체인 세션 영역)
- BMU `currentSoh` 정확한 source는 여전히 미정 — chaincode `CreatePassport` 초기값 100이 그대로 노출 중
