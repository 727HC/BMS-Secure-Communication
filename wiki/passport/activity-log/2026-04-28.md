---
title: "2026-04-28 Passport 활동 로그"
date: 2026-04-28
tags: [passport, log, frontend, ui]
doc_type: log
status: historical
---
# 2026-04-28 Passport 활동 로그

## Session 1 — 페이지 헤드 대시보드 정합 + 영문 eyebrow 제거
- 작업 주체: Claude Opus 4.7
- 작업 내용: 사용자 피드백("다른 페이지 상단이 대시보드와 다르다", "영어 적힌 거 굳이 필요 없다")에 따라 모든 비대시보드 페이지의 PageHead를 대시보드 `vk-dash__head` 스타일로 정합. 9개 페이지에서 영문 eyebrow(`Operational console`, `Service docket`, `Audit register`, `Lifecycle register`, `Field identify`, `Battery passport dossier` ×2, `Manufacturer filing desk`, `설정 기록`)를 모두 제거하고, `.sn-page-head` 박스 모델을 `.vk-dash__head`와 동일하게(min-height 54, gap 24, flex-start, flex-wrap, padding-right 240) 맞췄다. `.sn-page-subtitle`도 13px/500/1.5로 통일.
- 변경 파일: `webapp/frontend-react/src/styles/sn-page.css`, `webapp/frontend-react/src/styles/vk-dashboard.css`, `webapp/frontend-react/src/pages/{AuditLogPage,BmuDataPage,MaintenancePage,MaterialsPage,PassportDetailPage,PassportsPage,QrScanPage,RecyclingPage,SettingsPage}.tsx`.
- 검증: `cd webapp/frontend-react && npm run build` 통과, Playwright 1440×900 6개 페이지(dashboard/passports/maintenance/recycling/audit/bmu) 캡처 후 head computed style 비교 — 모두 title 24px/800/-0.6px/26.4lh + sub 13px/500/19.5lh 동일.
- 커밋: `08e490c refactor(frontend): 페이지 헤드를 대시보드 스타일로 통일`.

## Session 2 — PageHead actions에서 KPI mini 박스 제거
- 작업 주체: Claude Opus 4.7
- 작업 내용: 사용자 피드백("KPI 박스 때문에 head 위아래 여백이 크다, 아래 박스로 내려달라")에 따라 7개 페이지(passports, recycling, maintenance, materials, audit, bmu, qr) actions slot에서 `sn-kpi-mini` 박스("현재 표시 N", "현재 페이지 N", "현재 기록 N", "Lookup state") 일괄 제거. 카운트 정보는 페이지 본문 테이블/카드에서 이미 노출되므로 head는 필수 액션 버튼(발급 접수, 새로고침, Auto refresh 토글 등)만 유지.
- 변경 파일: `webapp/frontend-react/src/pages/{PassportsPage,RecyclingPage,MaintenancePage,MaterialsPage,AuditLogPage,BmuDataPage,QrScanPage}.tsx` (7 files, 75 deletions / 23 insertions).
- 검증: `npm run build` 통과 (286ms, TS 0건), Playwright 캡처에서 head 높이가 title+subtitle 영역에 맞춰져 슬림화됨을 확인. floating topbar와 actions 겹침 없음.
- 커밋: `994f7f8 refactor(frontend): PageHead actions에서 KPI mini 박스 제거`.

## 누적 commit chain (이번 세션, velkern-brand-assets)
- `bcb2716 fix(layout): PageHead actions와 floating topbar 레이아웃 정리`
- `e9df841 fix(layout): floating topbar 폭 축소 + PageHead 단일 행 정렬`
- `e480e53 refactor(layout): PageHead eyebrow 색상 통일 (페이지별 색상 차이 제거)`
- `08e490c refactor(frontend): 페이지 헤드를 대시보드 스타일로 통일`
- `994f7f8 refactor(frontend): PageHead actions에서 KPI mini 박스 제거`

## 미완료/주의
- `sn-section-card` 내부 영문 eyebrow(`MANUFACTURER FILING DESK`, `LIFECYCLE REGISTER VIEW` 등)는 페이지 head가 아닌 카드 내부 라벨이라 이번 스코프에서 제외. 추후 한국어화 또는 제거 검토 필요.
- 수정 범위는 `webapp/frontend-react/`에 한정. `chaincode/`, `passport-network/`, `embedded/`, `mcp-monitor/` 미접촉.
