---
title: "배터리 여권 세션 활동 로그 — 2026-04-17"
date: 2026-04-17
tags: [passport, log]
doc_type: log
---
# 배터리 여권 세션 활동 로그 — 2026-04-17

## Session 1 — 2026-04-17 (OpenCode)

### 작업 주체
- OpenCode 주도 작업

### 요약
`fix/sentinel-review-round1` 브랜치에서 legacy BATP 화면의 리뷰 지적사항 중 페이지 계약 불일치와 암묵 전역 의존을 정리했다. `pageProps` 전달 계약을 대시보드/등록부/상세에 맞추고, 상세 화면의 원자재 이동 prop 이름을 통일했다.

### 작업 내용
- `webapp/frontend/pages/dashboard.js`
  - `pageProps` prop을 선언했다.
  - `scaleSOC`, `scaleTemp`, `getStatusBadge`를 로컬 helper로 명시해 암묵 전역 의존을 제거했다.
- `webapp/frontend/pages/passports.js`
  - `pageProps` prop을 선언했다.
  - 초기 필터/정렬값을 `props.pageProps`에서 읽도록 바꾸고, route prop 변경 시 상태를 동기화하도록 watcher를 추가했다.
  - `scaleSOC`, `getStatusBadge`를 로컬 helper로 명시했다.
- `webapp/frontend/pages/passport-detail.js`
  - `pageProps` prop을 선언했다.
  - `window.__pageProps` fallback을 제거하고 hash + `props.pageProps` 기준으로 상세 route state를 동기화하도록 정리했다.
  - identity section의 `openMaterialPage` prop 이름과 부모 바인딩을 통일했다.

### 변경 파일(핵심)
- `webapp/frontend/pages/dashboard.js`
- `webapp/frontend/pages/passports.js`
- `webapp/frontend/pages/passport-detail.js`
- `wiki/passport/activity-log.md`
- `wiki/passport/activity-log/2026-04-17.md`

### 검증
- `node -c /home/heechan/bms-blockchain/webapp/frontend/pages/dashboard.js`
- `node -c /home/heechan/bms-blockchain/webapp/frontend/pages/passports.js`
- `node -c /home/heechan/bms-blockchain/webapp/frontend/pages/passport-detail.js`
- component contract smoke test
  - `dashboard-page`, `passports-page`, `passport-detail-page`가 모두 `pageProps` prop을 선언하는지 확인
  - `passport-detail-identity-section`이 `openMaterialPage` prop/call을 쓰는지 확인
  - 결과: `component-contract-ok`
- grep 확인
  - 대상 페이지에서 `window.__pageProps`, `openMaterialsPage` 잔존 없음
- LSP 진단 시도
  - 결과: `typescript-language-server` 미설치로 실행 불가

### 미완료
- 브라우저 기반 수동 QA와 Sentinel 재리뷰 loop는 후속 실행 필요
- `report.md`의 템플릿 보간 오류 항목은 현재 파일 상태에서 재현되지 않아 이번 라운드 수정 범위에 포함하지 않음

### 교훈
- legacy 페이지도 route prop 계약을 명시해 두면 hash fallback과 전역 변수 의존이 줄어든다.
- 리뷰 리포트는 그대로 치환하지 말고 현재 코드에 남아 있는지 다시 확인한 뒤 좁게 고치는 편이 안전하다.
