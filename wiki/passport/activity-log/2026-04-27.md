---
title: "2026-04-27 Passport 활동 로그"
date: 2026-04-27
tags: [passport, log]
doc_type: log
status: historical
---
# 2026-04-27 Passport 활동 로그

## Session 1 — non-dashboard route contract baseline
- 작업 주체: Sisyphus-Junior
- 작업 내용: Task 1 범위에서 `Layout.tsx`의 `Recycling & ESG` 라벨 계약을 고정하고, 기존 비대시보드 페이지에 안정적인 `data-page` 루트 selector를 추가했다.
- 변경 파일: `webapp/frontend-react/src/components/layout/Layout.tsx`, `webapp/frontend-react/src/pages/*Page.tsx` 중 landing/login/passports/passport-detail/materials/bmu-data/maintenance/recycling/qr-scan/audit-log, `.sisyphus/evidence/non-dashboard-mockup-pages/task-1-route-contract.json`, `.sisyphus/notepads/non-dashboard-mockup-pages/*.md`.
- 검증: `npm run build` 통과, 수정 TSX LSP 오류 0건, Playwright route-shell 확인 완료. 실제 E2E 로그인 비밀은 없어 client storage auth로 shell/selector 보존만 확인했다.
- 미완료/주의: `/settings` 라우트와 실제 Settings page는 Task 2 범위다. POST/PUT mutation QA와 real-backend evidence 수집은 Task 12 범위다.

## Session 11 — non-dashboard Playwright smoke
- 작업 주체: Sisyphus-Junior
- 작업 내용: UI-only fixture 기반 `non_dashboard_mockup_pages.spec.js`를 추가해 `/`, `/login`, 비대시보드 보호 라우트, `/settings`, `/dashboard` 렌더를 확인하고, 인증 없는 `/settings`/`/passports` 리다이렉트, desktop/mobile overflow, 다크모드, 역할별 action gate를 검증했다. 모바일 `/passports` overflow가 드러나 `layout.css`, `sn-card.css`, `sn-page.css`에 non-dashboard 반응형 보정을 추가했다.
- 변경 파일: `e2e-tests/tests/non_dashboard_mockup_pages.spec.js`, `webapp/frontend-react/src/styles/layout.css`, `webapp/frontend-react/src/styles/sn-card.css`, `webapp/frontend-react/src/styles/sn-page.css`.
- 검증: `node -c /home/heechan/bms-blockchain/e2e-tests/tests/non_dashboard_mockup_pages.spec.js` 통과, `cd /home/heechan/bms-blockchain/webapp/frontend-react && npm run build` 통과, `PW_BASE_URL=http://127.0.0.1:4187 /home/heechan/bms-blockchain/e2e-tests/node_modules/.bin/playwright test /home/heechan/bms-blockchain/e2e-tests/tests/non_dashboard_mockup_pages.spec.js --config /home/heechan/bms-blockchain/e2e-tests/playwright.config.js --workers=1` 6/6 통과.
- 미완료/주의: 이 smoke는 `page.route('**/api/**')`와 client storage auth를 쓰는 UI-only 증거이며 실제 backend/login/authorization 증거가 아니다. `/dashboard`는 방문과 다크모드 렌더만 확인했고 dashboard source/CSS는 수정하지 않았다.

## Session 12 — non-dashboard real backend contract evidence
- 작업 주체: Sisyphus-Junior
- 작업 내용: `non_dashboard_real_backend.spec.js`를 추가해 실제 `bmu-agent` HTTP만 사용하는 비대시보드 backend contract evidence를 수집하도록 했다. API mocking 없이 `/api/auth/login`, passports, materials, BMU records, audit, `/settings` no-API 계약, 권한 거부, 직렬 mutation 계약을 기록한다.
- 변경 파일: `e2e-tests/tests/non_dashboard_real_backend.spec.js`, `.sisyphus/evidence/task-12-non-dashboard-real-backend/real-backend-contract.json`, `.sisyphus/notepads/non-dashboard-mockup-pages/learnings.md`, `.sisyphus/notepads/non-dashboard-mockup-pages/issues.md`.
- 검증: `node -c /home/heechan/bms-blockchain/e2e-tests/tests/non_dashboard_real_backend.spec.js` 통과, LSP error 0건, 금지 패턴 검색(`page.route('**/api/**')`, `route.fulfill`, mock fixture response) 0건, `PW_BASE_URL="http://127.0.0.1:3001" npx playwright test non_dashboard_real_backend.spec.js --config=playwright.config.js --reporter=list --workers=1` 1/1 통과.
- 미완료/주의: 로컬 `bmu-agent` env를 로드한 최종 재검증에서 `workers=1`, `apiMockingUsed=false`, `blockerCount=0`, `contractViolationCount=0`, `allRequiredContractsObserved=true`를 기록했다. `POST /api/passports`, `POST /api/materials` 직렬 mutation과 `/qr-scan` manual lookup도 실제 backend 응답으로 확인했다.
- 리뷰 후 보완: `/maintenance`와 `/recycling`의 route-specific `GET /api/passports` evidence를 추가했고, evidence redaction과 non-loopback mutation opt-in guard를 보강했다. 최종적으로 `x-bms-dev-seed`는 `seedHeaderPresent`만 기록하고, `workers !== 1`은 테스트 실패로 처리한다. 보완 후 `node -c`, LSP error 0건, 금지 패턴 검색 0건, Playwright `--workers=1` 1/1 통과를 재확인했다.

## Session 13 — public rollback and protected shell logo centralization
- 작업 주체: Sisyphus-Junior
- 작업 내용: 보호 shell 전용 `ShellBrandLink`를 추가하고 `/dashboard`의 `DashboardReferenceShell`과 비대시보드 `Layout`이 같은 clickable VELKERN logo link를 공유하도록 교체했다. 로고 CSS는 `layout.css`의 `.oc-shell-brand`/`.oc-shell-brand__mark`와 shell-logo-only 보정에 한정했다.
- 변경 파일: `webapp/frontend-react/src/components/layout/ShellBrandLink.tsx`, `webapp/frontend-react/src/components/layout/DashboardReferenceShell.tsx`, `webapp/frontend-react/src/components/layout/Layout.tsx`, `webapp/frontend-react/src/styles/layout.css`, `.sisyphus/notepads/public-pages-logo-rollback/learnings.md`, `.sisyphus/notepads/public-pages-logo-rollback/issues.md`.
- 검증: 수정 TSX LSP diagnostics 오류 0건, `cd /home/heechan/bms-blockchain/webapp/frontend-react && npm run build` 통과, `PW_BASE_URL=http://127.0.0.1:4173 npx playwright test tests/shell_brand_smoke.temp.spec.js --config=playwright.config.js` 1/1 통과 후 임시 spec 삭제 및 generated report 복원.
- 미완료/주의: Playwright MCP inline smoke는 connection close로 중단되어 기존 `e2e-tests` runner를 사용했다. 제품 변경 관련 미완료 이슈는 없다.
- 최종 검증 추가: 사용자 요청 범위대로 public `/`와 `/login`은 rollback 상태를 유지하고, protected shell은 `ShellBrandLink` 기반 VELKERN 로고를 공유하는지 재확인했다.
- 최종 변경 파일 범위: `webapp/frontend-react/src/pages/LandingPage.tsx`, `webapp/frontend-react/src/pages/LoginPage.tsx`, `webapp/frontend-react/src/components/layout/ShellBrandLink.tsx`, `webapp/frontend-react/src/components/layout/DashboardReferenceShell.tsx`, `webapp/frontend-react/src/components/layout/Layout.tsx`, `webapp/frontend-react/src/styles/layout.css`, `e2e-tests/tests/non_dashboard_mockup_pages.spec.js`, `e2e-tests/tests/public_pages_logo_rollback.spec.js`, `.sisyphus/notepads/public-pages-logo-rollback/*.md`, `.sisyphus/evidence/public-pages-logo-rollback/final-verification-2026-04-27.json`.
- 최종 검증 명령: `npm run build` 통과, `node -c /home/heechan/bms-blockchain/e2e-tests/tests/non_dashboard_mockup_pages.spec.js` 통과, `node -c /home/heechan/bms-blockchain/e2e-tests/tests/public_pages_logo_rollback.spec.js` 통과, `PW_BASE_URL=http://127.0.0.1:3001 npx playwright test tests/non_dashboard_mockup_pages.spec.js tests/public_pages_logo_rollback.spec.js --config playwright.config.js --workers=1` 10/10 통과.
- 범위/보안: backend, route name, auth context, dependency, chaincode, passport-network 변경 없음. `.env`, token, credential, secret 출력이나 기록 없음. `ui_polish_smoke.spec.js`는 live backend/login dependency가 필요한 범위라 이번 public/logo 결정론적 UI-only 검증에는 포함하지 않았다.
- Wiki 검증: `/mnt/c/Users/heechan/Documents/BMS-Knowledge` mirror에 `rsync -a --update --exclude='.obsidian' --exclude='.trash'`로 동기화한 뒤 `python3 scripts/verify_wiki.py` 통과. Windows mirror parity도 `parity OK`로 확인했다.

## Session 14 — atomic commit 분할 + origin push (Claude Opus)
- 작업 주체: 사용자 + Claude Opus 4.7
- 작업 내용: Sisyphus 다세션 누적분(워킹트리 22개 파일)을 8개 atomic commit으로 분할 후 `origin/velkern-brand-assets`에 first push.
- Commit chain (master 기준): `e56cad5 feat(layout)` → `795ee47 refactor(dashboard)` → `23e93a1 feat(routing)` → `fe6c21b refactor(non-dashboard)` → `375dde3 feat(bmu-agent dev seed)` → `7c48fd4 test(e2e specs)` → `f2b4644 docs(activity-log)` → `8668c03 chore(deps biome)`.
- 검증: `npm run build` 통과 (340ms, TypeScript 에러 0건), `git push -u origin velkern-brand-assets` 신규 브랜치 등록.
- 미완료/주의: `.sisyphus/`(런타임 산출물)는 untracked 유지. master 머지는 별도 PR 단위로 분할 권고 (정확히 8 commit + Stage 1/2가 자연스러운 PR 경계).

## Session 15 — Stage 1: bmu-agent chaincode 에러 미들웨어
- 작업 주체: Claude Opus 4.7
- 작업 내용: `wiki/blockchain/chaincode-error-contract.md @cbd2304` §2 매핑 함수를 `bmu-agent/middleware/chaincode-error.js`로 이식해 7개 route catch 블록을 통일.
- 핵심 변경:
  - 신규 미들웨어: `unwrapFabricError` (Fabric wrap prefix 4종 다중 strip), `mapChaincodeError` (AUTHZ/VAL/NOT_FOUND/CONFLICT/PRECONDITION/INTERNAL → 403/400/404/409/409/500), `sendChaincodeError` (응답 스키마 `{ error, category }` 통일).
  - 헤더 sync 표식: `// synced from chaincode-error-contract.md @cbd2304` (§5.1 운영 규칙 3 준수).
  - 7개 route 통일: `passport.routes.js`, `bmu.routes.js`, `vc.routes.js`, `maintenance.routes.js`, `analysis.routes.js`, `recycling.routes.js`, `material.routes.js`. 기존 임시 헬퍼 `mapPassportErrorStatus` 제거.
  - agent-local 에러 보존: 서명 검증 401, DID lookup 502, validation 400.
- 검증: 매핑 함수 28 테스트 케이스 통과 (8 카테고리 × Fabric wrap 시나리오), 모든 route `node -c` 통과.
- 변경 파일: `bmu-agent/middleware/chaincode-error.js`(NEW), `bmu-agent/routes/{passport,bmu,vc,maintenance,analysis,recycling,material}.routes.js`.
- Commit: `ed2b977 feat(bmu-agent): chaincode 에러 매핑 미들웨어 + 7개 route 통일`. origin push 완료.
- 미완료/주의: chaincode 변경 무관 — caliper 독립으로 선행 머지 가능.

## Session 16 — Stage 2: webapp 한국어 토스트 dictionary
- 작업 주체: Claude Opus 4.7
- 작업 내용: `chaincode-error-contract.md @cbd2304` §4 한국어 dictionary를 `webapp/frontend-react/src/lib/chaincodeErrorMessages.ts`로 이식하고 4개 mutation 페이지 catch 블록을 통일.
- 핵심 변경:
  - `lib/api.ts`: `ApiError` 클래스 + `ApiErrorCategory` 타입 추가, fetch 응답에서 `category` 보존.
  - `lib/chaincodeErrorMessages.ts` 신규: 카테고리별 fallback + raw 메시지 prefix 정밀 매칭(SOH 범위, 빈 값, 여권 상태 가드 등 30+ 케이스). 헤더 `// synced from chaincode-error-contract.md @cbd2304`.
  - `MaintenancePage.tsx`, `RecyclingPage.tsx`, `MaterialsPage.tsx`: 기존 `// toast 생략` / `/* noop */` 모두 제거 → `toastFromError` 기반 `submitError` 상태 + inline `role="alert"` 배너 (`color-danger-soft` 토큰).
  - `PassportDetailPage.tsx`: `withSubmit` 헬퍼를 `toastFromError`로 마이그레이션 (raw `err.message` 노출 → 카테고리별 한국어 토스트 + `console.warn` 디버그 분리).
  - 모든 mutation 진입 시 `setSubmitError(null)` reset 추가.
- 검증: `npm run build` 통과 (274ms, TypeScript 에러 0건).
- 변경 파일: `webapp/frontend-react/src/lib/api.ts`, `webapp/frontend-react/src/lib/chaincodeErrorMessages.ts`(NEW), `webapp/frontend-react/src/pages/{PassportDetail,Maintenance,Recycling,Materials}Page.tsx`.
- Commit: `d8d1f1c feat(webapp): chaincode 에러 한국어 토스트 dictionary + mutation 페이지 통일`. origin push 완료.
- 미완료/주의: chaincode strict rejection이 사용자에게 한국어 토스트로 도달함을 코드 수준으로 확인. 실제 chaincode 거부 응답을 받는 e2e fixture 검증은 Stage 3 (caliper 마커 commit 신호 후 진행).

## 세션 간 협의 요약 (2026-04-27 Blockchain ↔ Passport, 6라운드)
- Round 1~2: Blockchain 측이 `chaincode-contract-audit-2026-04-27.md` + P0/P1 hardening (4f2bb88 ~ 0c2dccf) 공유. Passport 측이 e2e 픽스처 점검(위반 0건), UI 토스트 갭 식별, P2-1 단일 Service MSP 회신, agent route 사전 검증 보류 회신.
- Round 3: Blockchain `chaincode-error-contract.md` (311a48e) 138 unique 에러 템플릿 + §2 매핑 함수 + §4 한국어 dictionary 제공. Passport 측 sync 모델 결정 요청.
- Round 4: Blockchain §5.1 (b9acaf7)로 sync 책임 분담 확정 — chaincode + wiki §2/§3은 blockchain 한 commit, agent 미들웨어 + UI dictionary는 Passport sync. BREAKING 표식 + 헤더 SHA 운영 규칙 4건.
- Round 5: Blockchain `cbd2304` BREAKING — `failed to unmarshal recycling rates:` → `invalid recycling rates JSON:` 비대칭 정리. §3.6 예외 paragraph 제거. Passport Stage 1 머지 권고.
- Round 6: Blockchain `e582cf5` — wiki §2.1 Fabric wrap strip 부록 추가 + Stage 1/2 인지 + caliper 진입 5단계 절차 명시. 양 세션 휴식 모드 합의.
- 최종 단일 브랜치 상태: `origin/velkern-brand-assets`에 Blockchain 7 commit (P0/P1/P1.5/audit/error contract/§5.1/cbd2304/e582cf5) + Passport 10 commit (8 atomic dashboard + Stage 1/2). 정합 확인 완료.

## 다음 진입 신호 (caliper 시점)
- 신호 marker: `wiki/blockchain/activity-log.md`에 "Session 2026-04-XX caliper start" commit (Blockchain 측이 caliper write/read regression 측정 직전에 박음).
- 그 시점 Passport Stage 3 진행:
  - e2e 거부 케이스 픽스처 추가 (음수 SOH, 외부 회사 여권 정정 시도, 잘못된 enum 등 chaincode strict rejection 검증).
  - 한국어 토스트가 실제 chaincode 응답에 대해 정확히 매핑되는지 Playwright로 확인.
  - 토큰/credential은 ephemeral shell variable로만 다루고 출력/저장 금지.

